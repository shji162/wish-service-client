{
  "name": "wish-service",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm i && npm run build && cd ../server && npm i && npm run build",
    "start": "npm run start --prefix server"
  }
}